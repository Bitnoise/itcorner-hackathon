# apps/

This directory will hold:

- `apps/api/` — Hono `^4` backend with Drizzle ORM and PostgreSQL
- `apps/web/` — React `^18` + Vite `^5` + TanStack Query + TanStack Router frontend

Do NOT scaffold these manually. The agent loop creates them during phase 4 based on the PRD.
